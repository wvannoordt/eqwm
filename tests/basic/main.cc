#include "hw2.h"
int main(void)
{
    // hw2::UserInput input;
    hw2::globalSettings.debugLevel = 5;
    hw2::WallModel model(10);
    // Array u = model["u"];
    // u[i] = ui
    // SolveResult result = model.Solve();
    // Array tau = model["tau"];
    // taui = tau[i]
}